# MVP 验收记录

验收日期：2026-08-29；话题、主题与环境隔离迭代：2026-08-31

## 2026-08-31 主题与环境隔离验收

- 保留当前浅色设计，恢复原深蓝夜航配色、淡网格与荧光黄绿强调色；桌面导航底部、手机右上角切换。
- 浏览器实测：切换、刷新记忆、同源多标签页同步正常；桌面及 390px / 320px 手机布局无横向溢出，事项时间编辑、话题和笔记编辑均可读。
- `dev` 用于开发，`master` 用于验收发布；保留原 `main` 历史基线。本次仅配置本地分支与正式运行模式，未部署到公网。
- 开发与正式服务分别使用 3001 / 3000 端口、独立构建目录、独立 SQLite 与截图目录；开发界面标记测试数据，并禁用提醒订阅与发送。
- 正式库已从旧库迁移，逐表核对记录、关联、话题及笔记；截图哈希与读取接口通过，Web Push 密钥保留。迁移前备份与旧目录均保留。
- 开发库未复制私人记录；环境目录冲突、符号链接 / 硬链接混用、环境标记错误和未标记的既有数据库均会阻止启动。
- ESLint、TypeScript、生产构建及 29 项回归测试通过；包括迁移保留数据、失败不启用新库、重复迁移不覆盖、测试提醒阻断、主题初始化及部署清单防夹带私人数据。
- 后续启动、迁移和发布步骤见[开发与发布](development-and-release.md)。

## 2026-08-31 话题迭代验收

- “话题 / 主题 / 专题”自然语言创建、表单创建与名称去重；创建指令本身不进入待办。
- 输入时手动指定、AI 自动判断、保留未归类；支持文字、链接及截图。
- 话题页展示多个独立事项，待处理 / 已完成分组，支持修改话题名称和描述。
- 手动归属优先，包括明确选择未归类；既有事项可移动，时间、来源、提醒、建议和独立笔记副本保持不变。
- 独立临时数据库 + 模拟模型的 15 项回归测试通过，包含旧库迁移和跨项目防误合并。
- 独立空库使用真实 DeepSeek 验证 6 个场景通过：两条官网任务、门店任务、官网任务时间补充、无关买牛奶事项、手动指定话题。
- 浏览器实测：新建话题、连续录入两个独立事项、重新归类；桌面、390px 和 320px 手机布局无横向溢出。
- 修正话题切换的滚动位置、完成事项误操作入口，以及本地规则把“更新某文案”误当作补充的风险。
- 本轮测试不使用正式数据库，真实 DeepSeek 测试仅发送虚构项目和事项。

## 已完成闭环

1. 用户输入文字、链接或截图；
2. 原始内容在 AI 调用前保存到本地；
3. DeepSeek 提取事项、人物、场景、耗时和提醒时间；
4. AI 失败或未配置 Key 时回退到本地规则；
5. 事项进入今日、顺手处理、等待中或稍后；
6. 用户可以完成、延后、转为等待或放弃；
7. 后台调度器检查到期事项并通过 Web Push 提醒；
8. 新输入先与近期事项进行本地相关性筛选，再由 AI 判断新建或补充；
9. 多次补充保留独立原始来源，AI 结论可以追溯到原始文字或截图。
10. 方法、清单和建议作为便笺附到原事项，不进入待办队列；
11. 主动建议经过必要性判断，简单琐事不会被过度扩写。
12. 事项时间可以直接修改或清除，实际提醒触发器同步更新；
13. AI 建议可以删除，或复制到独立笔记本继续查看；笔记副本可独立编辑和删除。

## 已执行检查

- DeepSeek 文本解析真实调用：通过；
- DeepSeek 截图视觉解析真实调用：通过；
- 人物、生活关系和场景提取：通过；
- 自然语言时间窗口：修正并复测“明天下班后”和“周末”；
- 上下文补充合并：使用“先记录事项、稍后补充具体时间”真实调用通过；
- 建议附加：输入“查找做披萨的方法”后关联到已有披萨事项，未创建新任务；
- 选择性主动建议：复杂食材采购会附建议，“倒垃圾”不附建议；
- 按需生成：事项内“给我建议”入口真实调用通过；
- 时间修正：修改事项时间后，事项、唯一生效触发器和反馈记录一致；
- 建议管理：删除与复制到笔记本接口通过，复制后事项建议保持不变；
- 笔记本：标题、摘要、正文编辑与二次确认删除通过，桌面端和 390px 手机端布局通过；
- 完成与延后状态流转：通过；
- 原始截图读取：通过；
- 空数据状态：通过；
- 桌面端和移动端响应式布局：通过；
- TypeScript 类型检查：通过；
- ESLint：通过；
- Next.js 生产构建：通过；
- 生产依赖安全审计：未发现已知漏洞；
- 测试事项与测试附件：已清理。

## MVP 边界

- 当前是单用户应用，没有登录系统，只适合本机、家庭网络或受保护的私有环境；
- 外网部署需要 HTTPS，且应先增加身份认证；
- Web Push 需要用户在浏览器点击铃铛并允许通知；
- 后台调度依赖长期运行的 Node.js 服务，不适合会随时休眠的无服务器部署；
- 链接当前会保存并提取 URL 上下文，但不会主动抓取受登录保护的页面内容；
- 习惯、生活关系和个人规则已有数据模型与产品设计，编辑页面留到下一阶段；
- 发送给 DeepSeek 的是当前输入、最多六条本地预筛选的相关事项摘要及最多 40 个话题的名称与描述，不是完整个人数据库。
- 建议当前基于模型知识生成并明确标注“未联网核验”；实时搜索和来源引用尚未接入。

## 试用重点

试用时优先观察：

- 是否能在十秒内完成一次捕获；
- AI 对“谁应该做什么”的判断是否准确；
- 提醒时机是否真的合适；
- 哪些事项经常被延后；
- 哪些固定习惯、人物关系或生活场景最值得优先录入系统。
