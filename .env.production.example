# 可复制为 .env.production.local；npm run build 和 npm start 加载。
PRODUCTION_DATA_DIR=./data/production
ENABLE_REMINDERS=true
# 如需正式环境单独的 DeepSeek Key，在本文件的本地副本里配置 DEEPSEEK_API_KEY。
# 默认继承 .env.local 中的 Key。
